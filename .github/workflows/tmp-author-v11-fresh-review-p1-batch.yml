name: Temporary author V11 fresh-review P1 remediation

on:
  push:
    branches:
      - stage/hms-superpowers-v0.2.0-ui-design-authority
    paths:
      - .github/workflows/tmp-author-v11-fresh-review-p1-batch.yml

permissions:
  contents: write

jobs:
  author:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-2025
    env:
      CLEAN_BASELINE: f627df23dfc65b8f4cd8ecb2192c9bfccfdd69f5
      EXPECTED_PARENT: f627df23dfc65b8f4cd8ecb2192c9bfccfdd69f5
      SELF_PATH: .github/workflows/tmp-author-v11-fresh-review-p1-batch.yml
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Bind V11 author to exact frozen reviewed candidate
        shell: pwsh
        run: |
          $ErrorActionPreference='Stop'
          $parent=((& git rev-parse HEAD^)-join '').Trim().ToLowerInvariant()
          if($LASTEXITCODE -ne 0 -or $parent -cne $env:EXPECTED_PARENT){throw "V11 parent mismatch. Expected $env:EXPECTED_PARENT, found $parent"}
          $changed=@(& git diff --name-only HEAD^ HEAD)
          if($changed.Count -ne 1 -or [string]$changed[0] -cne $env:SELF_PATH){throw "V11 bootstrap scope mismatch: $($changed -join ', ')"}

      - name: Apply exact four-P1 remediation batch
        shell: python
        run: |
          from pathlib import Path

          def load(path): return Path(path).read_text(encoding='utf-8')
          def save(path,text): Path(path).write_text(text,encoding='utf-8',newline='\n')
          def once(text,old,new,label):
              n=text.count(old)
              if n != 1: raise SystemExit(f'{label}: expected exactly one anchor, found {n}')
              return text.replace(old,new,1)
          def insert_before(text,anchor,snippet,label):
              return once(text,anchor,snippet+anchor,label)
          def replace_between(text,start,end,replacement,label):
              a=text.find(start)
              if a < 0 or text.find(start,a+1) >= 0: raise SystemExit(f'{label}: start anchor missing/duplicated')
              b=text.find(end,a+len(start))
              if b < 0: raise SystemExit(f'{label}: end anchor missing')
              return text[:a]+replacement+text[b:]

          lifecycle_old="    & (Join-Path $InstallRoot 'scripts\\Test-HmsSkills.ps1')\n    & (Join-Path $InstallRoot 'scripts\\Test-DeliveryTools.ps1')\n"
          lifecycle_new="    # Developer/CI test harnesses are intentionally not executable from the trusted production lifecycle.\n    # Permanent CI qualifies those validators on exact committed release candidates.\n"
          for p in ('install.ps1','update.ps1'):
              t=load(p); t=once(t,lifecycle_old,lifecycle_new,f'{p} developer-validator removal'); save(p,t)

          native_child=r'''    public sealed class HmsChildEntry
    {
        public string Name;
        public ulong FileId;
        public uint Attributes;
        public HmsChildEntry(string name, ulong fileId, uint attributes) { Name=name; FileId=fileId; Attributes=attributes; }
    }
    [DllImport("kernel32.dll",SetLastError=true)] [return:MarshalAs(UnmanagedType.Bool)]
    private static extern bool GetFileInformationByHandleEx(SafeFileHandle handle,int infoClass,IntPtr info,uint size);
    public static HmsChildEntry[] EnumerateChildrenByHandle(SafeFileHandle handle,out int error)
    {
        var result=new System.Collections.Generic.List<HmsChildEntry>();
        const int bufferSize=65536;
        IntPtr buffer=Marshal.AllocHGlobal(bufferSize);
        try
        {
            bool restart=true;
            while(true)
            {
                bool ok=GetFileInformationByHandleEx(handle,restart?11:10,buffer,(uint)bufferSize);
                restart=false;
                if(!ok)
                {
                    int code=Marshal.GetLastWin32Error();
                    if(code==18){error=0;break;}
                    error=code;return null;
                }
                int offset=0;
                while(true)
                {
                    IntPtr entry=IntPtr.Add(buffer,offset);
                    uint next=unchecked((uint)Marshal.ReadInt32(entry,0));
                    uint attrs=unchecked((uint)Marshal.ReadInt32(entry,56));
                    uint nameBytes=unchecked((uint)Marshal.ReadInt32(entry,60));
                    ulong fileId=unchecked((ulong)Marshal.ReadInt64(entry,96));
                    if(nameBytes>32768 || (nameBytes&1)!=0){error=13;return null;}
                    string name=Marshal.PtrToStringUni(IntPtr.Add(entry,104),(int)(nameBytes/2));
                    if(name!="." && name!="..") result.Add(new HmsChildEntry(name,fileId,attrs));
                    if(next==0)break;
                    if(next<104 || offset+(long)next>=bufferSize){error=13;return null;}
                    offset+=(int)next;
                }
            }
            error=0;return result.ToArray();
        }
        finally{Marshal.FreeHGlobal(buffer);}
    }
'''

          def child_helper(cls,fn,delete_method):
              return f'''function {fn} {{
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)][string]$Label)
    if ($null -eq $Guard.Handle -or $Guard.Handle.IsClosed -or $Guard.Handle.IsInvalid) {{ throw "$Label exact parent handle is unavailable for child enumeration." }}
    $enumCode=0
    $entries=@([{cls}]::EnumerateChildrenByHandle($Guard.Handle,[ref]$enumCode))
    if ($enumCode -ne 0) {{ throw "$Label exact child enumeration failed (Win32=$enumCode): $($Guard.Path)" }}
    $probe=[string]$env:HMS_TEST_EXACT_CHILD_ENUM_READY
    if (-not [string]::IsNullOrWhiteSpace($probe) -and $entries.Count -gt 0) {{
        $env:HMS_TEST_EXACT_CHILD_ENUM_READY=''
        [IO.File]::WriteAllText($probe,(Join-Path $Guard.Path ([string]$entries[0].Name)),(New-Object Text.UTF8Encoding($false)))
        Start-Sleep -Milliseconds 1200
    }}
    foreach ($entry in $entries) {{
        $name=[string]$entry.Name
        if ([string]::IsNullOrWhiteSpace($name) -or $name -in @('.','..') -or $name.Contains('\\') -or $name.Contains('/')) {{ throw "$Label exact child enumeration returned an unsafe name: $name" }}
        $childPath=Join-Path $Guard.Path $name
        $expectedDirectory=(([uint32]$entry.Attributes -band [uint32]0x10) -ne 0)
        $expectedReparse=(([uint32]$entry.Attributes -band [uint32]0x400) -ne 0)
        $access=[uint32]0x00010080
        if ($expectedDirectory -and -not $expectedReparse) {{ $access=[uint32]($access -bor [uint32]1) }}
        $child=[{cls}]::CreateFileW($childPath,$access,[uint32]3,[IntPtr]::Zero,[uint32]3,[uint32]0x02200000,[IntPtr]::Zero)
        if ($null -eq $child -or $child.IsInvalid) {{
            $code=[Runtime.InteropServices.Marshal]::GetLastWin32Error(); if($null -ne $child){{$child.Dispose()}}
            throw "$Label exact child open failed after enumeration (Win32=$code): $childPath"
        }}
        try {{
            $info=New-Object '{cls}+BY_HANDLE_FILE_INFORMATION'
            if (-not [{cls}]::GetFileInformationByHandle($child,[ref]$info)) {{ $code=[Runtime.InteropServices.Marshal]::GetLastWin32Error(); throw "$Label exact child identity read failed (Win32=$code): $childPath" }}
            $actualId=([uint64]$info.FileIndexHigh * [uint64]4294967296) + [uint64]$info.FileIndexLow
            if ($actualId -ne [uint64]$entry.FileId) {{ throw "$Label child identity changed between enumeration and exact-handle open; refusing foreign replacement: $childPath" }}
            $actualDirectory=(($info.FileAttributes -band [uint32]0x10) -ne 0)
            $actualReparse=(($info.FileAttributes -band [uint32]0x400) -ne 0)
            if ($actualDirectory -ne $expectedDirectory -or $actualReparse -ne $expectedReparse) {{ throw "$Label child type changed between enumeration and exact-handle open: $childPath" }}
            if ($actualDirectory -and -not $actualReparse) {{
                {fn} -Guard ([pscustomobject]@{{Handle=$child;Path=$childPath}}) -Label "$Label child '$name'"
            }}
            if (($info.FileAttributes -band [uint32]1) -ne 0) {{
                $attrs=[IO.File]::GetAttributes($childPath)
                [IO.File]::SetAttributes($childPath,($attrs -band (-bnot [IO.FileAttributes]::ReadOnly)))
            }}
            $deleteCode=0
            if (-not [{cls}]::{delete_method}($child,[ref]$deleteCode)) {{ throw "$Label exact child delete-pending transition failed (Win32=$deleteCode): $childPath" }}
            $child.Dispose(); $child=$null
            if (Test-Path -LiteralPath $childPath) {{ throw "$Label child pathname became occupied after exact-object deletion; refusing further destructive work: $childPath" }}
        }}
        finally {{ if ($null -ne $child) {{ $child.Dispose() }} }}
    }}
    $remainingCode=0
    $remaining=@([{cls}]::EnumerateChildrenByHandle($Guard.Handle,[ref]$remainingCode))
    if ($remainingCode -ne 0) {{ throw "$Label exact post-delete enumeration failed (Win32=$remainingCode): $($Guard.Path)" }}
    if ($remaining.Count -ne 0) {{ throw "$Label exact parent gained or retained children during cleanup; refusing root deletion: $($Guard.Path)" }}
}}

'''

          # Public builder and committed-copy helper share the HmsOwnedTempNative contract.
          for p in ('scripts/Build-HmsCompositeSkill.ps1','scripts/Copy-HmsCommittedGitPath.ps1'):
              t=load(p)
              t=insert_before(t,'    public static bool RenameHmsOwnedDirectoryByHandle',native_child,f'{p} native child enumeration')
              t=once(t,'        [uint32]$DesiredAccess = [uint32]0x00010000,','        [uint32]$DesiredAccess = [uint32]0x00010001,',f'{p} root list-directory access')
              t=insert_before(t,'function Get-HmsOwnedDirectoryIdentityFromHandle',child_helper('HmsOwnedTempNative','Remove-HmsOwnedExactChildren','DeleteHmsOwnedDirectoryByHandle'),f'{p} exact child helper')
              old="        foreach ($child in @(Get-ChildItem -LiteralPath $quarantine -Force -ErrorAction Stop)) {\n            Remove-Item -LiteralPath $child.FullName -Recurse -Force -ErrorAction Stop\n        }\n"
              new="        Remove-HmsOwnedExactChildren -Guard ([pscustomobject]@{Handle=$Owned.Guard;Path=$quarantine}) -Label $Label\n"
              t=once(t,old,new,f'{p} child pathname recursion removal')
              save(p,t)

          # Uninstall exact recursive deletion.
          p='uninstall.ps1'; t=load(p)
          t=insert_before(t,'    public static bool RenameByHandle',native_child,'uninstall native child enumeration')
          t=once(t,"$h=[HmsUninstallExactFsNative]::CreateFileW($Path,[uint32]0x00010000,[uint32]3","$h=[HmsUninstallExactFsNative]::CreateFileW($Path,[uint32]0x00010001,[uint32]3",'uninstall root list-directory access')
          t=insert_before(t,'function Get-HmsUninstallDirectoryIdentityFromHandle',child_helper('HmsUninstallExactFsNative','Remove-HmsUninstallExactChildren','DeleteByHandle'),'uninstall exact child helper')
          t=once(t,"        foreach ($child in @(Get-ChildItem -LiteralPath $q -Force -ErrorAction Stop)) { Remove-Item -LiteralPath $child.FullName -Recurse -Force -ErrorAction Stop }\n","        Remove-HmsUninstallExactChildren -Guard $guard -Label $Label\n",'uninstall child pathname recursion removal')
          save(p,t)

          # Delivery exact recursive deletion plus exact file publication primitives.
          p='scripts/Sync-DeliveryTools.ps1'; t=load(p)
          t=insert_before(t,'    public static bool RenameByHandle',native_child,'delivery native child enumeration')
          t=once(t,"$h=[HmsDeliveryExactFsNative]::CreateFileW($Path,[uint32]0x00010000,[uint32]3","$h=[HmsDeliveryExactFsNative]::CreateFileW($Path,[uint32]0x00010001,[uint32]3",'delivery root list-directory access')
          file_helpers=r'''function Get-HmsDeliveryFileIdentityFromHandle {
    param([Parameter(Mandatory)]$Handle,[Parameter(Mandatory)][string]$Label)
    $info=New-Object 'HmsDeliveryExactFsNative+BY_HANDLE_FILE_INFORMATION'
    if(-not[HmsDeliveryExactFsNative]::GetFileInformationByHandle($Handle,[ref]$info)){$code=[Runtime.InteropServices.Marshal]::GetLastWin32Error();throw "$Label could not read exact file identity (Win32=$code)."}
    if(($info.FileAttributes-band[uint32]0x10)-ne 0 -or ($info.FileAttributes-band[uint32]0x400)-ne 0){throw "$Label must be a regular non-reparse file."}
    return([string]$info.VolumeSerialNumber+':'+[string]$info.FileIndexHigh+':'+[string]$info.FileIndexLow)
}
function Open-HmsDeliveryFileGuard {
    param([Parameter(Mandatory)][string]$Path,[Parameter(Mandatory)][string]$Label)
    $h=[HmsDeliveryExactFsNative]::CreateFileW($Path,[uint32]0x80010000,[uint32]1,[IntPtr]::Zero,[uint32]3,[uint32]0x00200000,[IntPtr]::Zero)
    if($null -eq $h -or $h.IsInvalid){$code=[Runtime.InteropServices.Marshal]::GetLastWin32Error();if($null -ne $h){$h.Dispose()};throw "$Label could not open exact read+DELETE file handle (Win32=$code): $Path"}
    try{$id=Get-HmsDeliveryFileIdentityFromHandle -Handle $h -Label $Label;return[pscustomobject]@{Handle=$h;Identity=$id;Path=$Path}}catch{$h.Dispose();throw}
}
function Read-HmsDeliveryFileBytesFromGuard {
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)][string]$Label)
    if($null -eq $Guard.Handle -or $Guard.Handle.IsClosed -or $Guard.Handle.IsInvalid){throw "$Label exact file guard is unavailable for read."}
    if((Get-HmsDeliveryFileIdentityFromHandle -Handle $Guard.Handle -Label $Label)-cne[string]$Guard.Identity){throw "$Label exact file identity changed before read."}
    $bytes=[IO.File]::ReadAllBytes([string]$Guard.Path)
    if((Get-HmsDeliveryFileIdentityFromHandle -Handle $Guard.Handle -Label "$Label post-read")-cne[string]$Guard.Identity){throw "$Label exact file identity changed across read."}
    return[byte[]]$bytes
}
function Move-HmsDeliveryFileGuard {
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)][string]$Destination,[Parameter(Mandatory)][string]$Label)
    if((Get-HmsDeliveryFileIdentityFromHandle -Handle $Guard.Handle -Label $Label)-cne[string]$Guard.Identity){throw "$Label exact file identity changed before rename."}
    if(Test-Path -LiteralPath $Destination){throw "$Label destination is occupied: $Destination"}
    $code=0;if(-not[HmsDeliveryExactFsNative]::RenameByHandle($Guard.Handle,$Destination,[ref]$code)){throw "$Label exact file handle rename failed (Win32=$code): $Destination"}
    if((Get-HmsDeliveryFileIdentityFromHandle -Handle $Guard.Handle -Label "$Label post-rename")-cne[string]$Guard.Identity){throw "$Label exact file identity changed across rename."}
    $Guard.Path=$Destination
}
function Remove-HmsDeliveryFileGuard {
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)][string]$Label)
    if((Get-HmsDeliveryFileIdentityFromHandle -Handle $Guard.Handle -Label $Label)-cne[string]$Guard.Identity){throw "$Label exact file identity changed before deletion."}
    $code=0;if(-not[HmsDeliveryExactFsNative]::DeleteByHandle($Guard.Handle,[ref]$code)){throw "$Label exact file delete-pending transition failed (Win32=$code): $($Guard.Path)"}
    $Guard.Handle.Dispose();$Guard.Handle=$null
}
function Test-HmsExactBytesEqual {
    param([byte[]]$Left,[byte[]]$Right)
    if($null -eq $Left -or $null -eq $Right -or $Left.Length -ne $Right.Length){return $false}
    for($i=0;$i -lt $Left.Length;$i++){if($Left[$i]-ne$Right[$i]){return $false}}
    return $true
}

'''
          t=insert_before(t,'function Get-HmsDeliveryPortableDirectoryIdentity',file_helpers+child_helper('HmsDeliveryExactFsNative','Remove-HmsDeliveryExactChildren','DeleteByHandle'),'delivery exact file/child helpers')
          t=once(t,"        foreach ($child in @(Get-ChildItem -LiteralPath $q -Force -ErrorAction Stop)) { Remove-Item -LiteralPath $child.FullName -Recurse -Force -ErrorAction Stop }\n","        Remove-HmsDeliveryExactChildren -Guard $guard -Label $Label\n",'delivery child pathname recursion removal')

          marker_block=r'''function Convert-CodeGraphJsonGuardToObject {
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)][string]$Label)
    $bytes=Read-HmsDeliveryFileBytesFromGuard -Guard $Guard -Label $Label
    $strict=New-Object Text.UTF8Encoding($false,$true)
    try{$text=$strict.GetString([byte[]]$bytes);$obj=$text|ConvertFrom-Json}catch{throw "$Label exact bytes are not valid UTF-8 JSON: $($_.Exception.Message)"}
    return[pscustomobject]@{Bytes=[byte[]]$bytes;Object=$obj}
}
function Assert-CodeGraphMarkerGuardIdentity {
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)]$Identity,[Parameter(Mandatory)][string]$Label)
    $record=Convert-CodeGraphJsonGuardToObject -Guard $Guard -Label $Label
    foreach($field in @('managed_by','artifact','transaction_id','role','version','tag','commit','asset','sha256','bundle_tree_sha256')){
        if([string]$record.Object.$field -cne [string]$Identity[$field]){throw "$Label marker mismatch for '$field'."}
    }
    return $record
}
function Write-CodeGraphBundleMarker {
    param([Parameter(Mandatory)][string]$Path,[Parameter(Mandatory)]$Identity,$ExpectedPreviousIdentity=$null)
    Assert-RegularCodeGraphBundle -Path $Path -Label 'CodeGraph transaction bundle'
    $treeHash=[string]$Identity['bundle_tree_sha256']
    if([string]::IsNullOrWhiteSpace($treeHash)){$treeHash=Get-CodeGraphBundleTreeSha256 -Path $Path;$Identity['bundle_tree_sha256']=$treeHash}
    if($treeHash -notmatch '^[0-9a-f]{64}$'){throw "CodeGraph transaction identity has invalid bundle tree SHA-256: $treeHash"}
    $markerPath=Join-Path $Path $CodeGraphBundleMarkerName
    $publishParent=Split-Path -Parent $Path
    $publishTemp=Join-Path $publishParent ('.hms-codegraph-marker-publish-'+[guid]::NewGuid().ToString('N')+'.tmp')
    $oldReserved=Join-Path $publishParent ('.hms-codegraph-marker-previous-'+[guid]::NewGuid().ToString('N')+'.tmp')
    $discard=Join-Path $publishParent ('.hms-codegraph-marker-discard-'+[guid]::NewGuid().ToString('N')+'.tmp')
    $json=$Identity|ConvertTo-Json -Depth 4
    $expectedBytes=(New-Object Text.UTF8Encoding($false)).GetBytes($json+"`n")
    $existingGuard=$null;$candidateGuard=$null;$oldMoved=$false;$candidatePublished=$false
    try{
        if(Test-Path -LiteralPath $markerPath){
            if($null -eq $ExpectedPreviousIdentity){throw "CodeGraph transaction marker unexpectedly exists without previous-identity authority: $markerPath"}
            $existingGuard=Open-HmsDeliveryFileGuard -Path $markerPath -Label 'Existing CodeGraph transaction marker'
            $null=Assert-CodeGraphMarkerGuardIdentity -Guard $existingGuard -Identity $ExpectedPreviousIdentity -Label 'Existing CodeGraph transaction marker'
            Move-HmsDeliveryFileGuard -Guard $existingGuard -Destination $oldReserved -Label 'Previous CodeGraph marker reservation'
            $oldMoved=$true
            $probe=[string]$env:HMS_TEST_CODEGRAPH_MARKER_RESERVED_READY
            if(-not[string]::IsNullOrWhiteSpace($probe)){[IO.File]::WriteAllText($probe,$markerPath,(New-Object Text.UTF8Encoding($false)));Start-Sleep -Milliseconds 1200}
        }elseif($null -ne $ExpectedPreviousIdentity){throw "Expected previous CodeGraph transaction marker disappeared before exact publication: $markerPath"}
        [IO.File]::WriteAllBytes($publishTemp,$expectedBytes)
        $candidateGuard=Open-HmsDeliveryFileGuard -Path $publishTemp -Label 'Candidate CodeGraph transaction marker'
        $candidateBytes=Read-HmsDeliveryFileBytesFromGuard -Guard $candidateGuard -Label 'Candidate CodeGraph transaction marker'
        if(-not(Test-HmsExactBytesEqual -Left $candidateBytes -Right $expectedBytes)){throw 'Candidate CodeGraph marker bytes changed before publication.'}
        Move-HmsDeliveryFileGuard -Guard $candidateGuard -Destination $markerPath -Label 'Candidate CodeGraph marker publication'
        $candidatePublished=$true
        $null=Assert-CodeGraphMarkerGuardIdentity -Guard $candidateGuard -Identity $Identity -Label 'Published CodeGraph transaction marker'
        if($null -ne $existingGuard){Remove-HmsDeliveryFileGuard -Guard $existingGuard -Label 'Previous CodeGraph marker disposal';$existingGuard=$null;$oldMoved=$false}
        $candidateGuard.Handle.Dispose();$candidateGuard.Handle=$null;$candidateGuard=$null
    }catch{
        $writeError=$_;$rollbackErrors=@()
        if($null -ne $candidateGuard){try{if($candidatePublished){Move-HmsDeliveryFileGuard -Guard $candidateGuard -Destination $discard -Label 'Candidate CodeGraph marker rollback reservation'};Remove-HmsDeliveryFileGuard -Guard $candidateGuard -Label 'Candidate CodeGraph marker rollback disposal';$candidateGuard=$null}catch{$rollbackErrors+="Candidate marker rollback failed: $($_.Exception.Message)"}}
        if($null -ne $existingGuard -and $oldMoved){try{if(Test-Path -LiteralPath $markerPath){throw "Canonical CodeGraph marker pathname became occupied during rollback: $markerPath"};Move-HmsDeliveryFileGuard -Guard $existingGuard -Destination $markerPath -Label 'Previous CodeGraph marker restoration';$null=Assert-CodeGraphMarkerGuardIdentity -Guard $existingGuard -Identity $ExpectedPreviousIdentity -Label 'Restored previous CodeGraph marker';$existingGuard.Handle.Dispose();$existingGuard.Handle=$null;$existingGuard=$null;$oldMoved=$false}catch{$rollbackErrors+="Previous marker restoration failed: $($_.Exception.Message)"}}
        if($rollbackErrors.Count){throw "CodeGraph marker publication failed and exact-object rollback was incomplete. Original: $($writeError.Exception.Message). Rollback: $($rollbackErrors -join ' | ')"}
        throw $writeError
    }finally{if($null -ne $candidateGuard -and $null -ne $candidateGuard.Handle){$candidateGuard.Handle.Dispose()};if($null -ne $existingGuard -and $null -ne $existingGuard.Handle){$existingGuard.Handle.Dispose()}}
}

'''
          t=replace_between(t,'function Write-CodeGraphBundleMarker {','function Assert-CodeGraphTransactionBundle {',marker_block,'exact marker publication')
          t=t.replace('Write-CodeGraphBundleMarker -Path $CurrentPath -Identity $PreviousIdentity','Write-CodeGraphBundleMarker -Path $CurrentPath -Identity $PreviousIdentity -ExpectedPreviousIdentity $BackupIdentity')
          t=t.replace('Write-CodeGraphBundleMarker -Path $CurrentPath -Identity $BackupIdentity','Write-CodeGraphBundleMarker -Path $CurrentPath -Identity $BackupIdentity -ExpectedPreviousIdentity $existingIdentity')
          t=t.replace('Write-CodeGraphBundleMarker -Path $CurrentPath -Identity $existingIdentity; Assert-CodeGraphTransactionBundle','Write-CodeGraphBundleMarker -Path $CurrentPath -Identity $existingIdentity -ExpectedPreviousIdentity $BackupIdentity; Assert-CodeGraphTransactionBundle')

          managed_block=r'''function Open-ManagedCodeGraphManifestState {
    if(-not(Test-Path -LiteralPath $CodeGraphRoot)){return $null}
    if(-not(Test-Path -LiteralPath $CodeGraphManifest)){throw "Refusing to overwrite existing CodeGraph path not owned by HMS Skills Codex: $CodeGraphRoot"}
    $guard=Open-HmsDeliveryFileGuard -Path $CodeGraphManifest -Label 'Managed CodeGraph manifest'
    try{
        $record=Convert-CodeGraphJsonGuardToObject -Guard $guard -Label 'Managed CodeGraph manifest'
        if([string]$record.Object.managed_by -cne $ManagedBy){throw "Unexpected CodeGraph installation owner at $CodeGraphRoot"}
        return[pscustomobject]@{Manifest=$record.Object;Bytes=[byte[]]$record.Bytes;Guard=$guard}
    }catch{$guard.Handle.Dispose();throw}
}
function Read-ManagedCodeGraphManifest {
    $state=Open-ManagedCodeGraphManifestState
    if($null -eq $state){return $null}
    try{return $state.Manifest}finally{if($null -ne $state.Guard -and $null -ne $state.Guard.Handle){$state.Guard.Handle.Dispose();$state.Guard.Handle=$null}}
}

'''
          t=replace_between(t,'function Read-ManagedCodeGraphManifest {','function Assert-CodeGraphVersion {',managed_block,'managed manifest exact read')

          manifest_helpers=r'''function Assert-CodeGraphInstallManifestGuard {
    param([Parameter(Mandatory)]$Guard,[Parameter(Mandatory)]$Manifest,[Parameter(Mandatory)][string]$Label)
    $record=Convert-CodeGraphJsonGuardToObject -Guard $Guard -Label $Label
    foreach($field in @('managed_by','version','tag','commit','asset','sha256','bundle_transaction_id','bundle_tree_sha256')){if([string]$record.Object.$field -cne [string]$Manifest[$field]){throw "$Label manifest mismatch for '$field'."}}
    return $record
}
function Publish-CodeGraphInstallManifest {
    param([Parameter(Mandatory)]$Manifest,$ExistingState)
    $parent=Split-Path -Parent $CodeGraphManifest
    $temp=Join-Path $parent ('.hms-codegraph-manifest-candidate-'+[guid]::NewGuid().ToString('N')+'.tmp')
    $previousReserved=Join-Path $parent ('.hms-codegraph-manifest-previous-'+[guid]::NewGuid().ToString('N')+'.tmp')
    $discard=Join-Path $parent ('.hms-codegraph-manifest-discard-'+[guid]::NewGuid().ToString('N')+'.tmp')
    $json=$Manifest|ConvertTo-Json
    $expected=(New-Object Text.UTF8Encoding($false)).GetBytes($json+"`n")
    $candidateGuard=$null;$oldMoved=$false;$candidatePublished=$false
    try{
        [IO.File]::WriteAllBytes($temp,$expected)
        $candidateGuard=Open-HmsDeliveryFileGuard -Path $temp -Label 'Candidate CodeGraph install manifest'
        if(-not(Test-HmsExactBytesEqual -Left (Read-HmsDeliveryFileBytesFromGuard -Guard $candidateGuard -Label 'Candidate CodeGraph install manifest') -Right $expected)){throw 'Candidate CodeGraph install manifest bytes changed before publication.'}
        if($null -ne $ExistingState){
            if($null -eq $ExistingState.Guard -or $ExistingState.Guard.Handle.IsClosed){throw 'Existing CodeGraph manifest exact guard is unavailable before publication.'}
            $null=Assert-CodeGraphInstallManifestGuard -Guard $ExistingState.Guard -Manifest $ExistingState.Manifest -Label 'Existing CodeGraph install manifest'
            Move-HmsDeliveryFileGuard -Guard $ExistingState.Guard -Destination $previousReserved -Label 'Previous CodeGraph install manifest reservation';$oldMoved=$true
            $probe=[string]$env:HMS_TEST_CODEGRAPH_MANIFEST_RESERVED_READY
            if(-not[string]::IsNullOrWhiteSpace($probe)){[IO.File]::WriteAllText($probe,$CodeGraphManifest,(New-Object Text.UTF8Encoding($false)));Start-Sleep -Milliseconds 1200}
        }
        Move-HmsDeliveryFileGuard -Guard $candidateGuard -Destination $CodeGraphManifest -Label 'Candidate CodeGraph install manifest publication';$candidatePublished=$true
        $null=Assert-CodeGraphInstallManifestGuard -Guard $candidateGuard -Manifest $Manifest -Label 'Published CodeGraph install manifest'
        if($null -ne $ExistingState){Remove-HmsDeliveryFileGuard -Guard $ExistingState.Guard -Label 'Previous CodeGraph install manifest disposal';$ExistingState.Guard=$null;$oldMoved=$false}
        $result=$candidateGuard;$candidateGuard=$null;return $result
    }catch{
        $writeError=$_;$rollbackErrors=@()
        if($null -ne $candidateGuard){try{if($candidatePublished){Move-HmsDeliveryFileGuard -Guard $candidateGuard -Destination $discard -Label 'Candidate CodeGraph install manifest rollback reservation'};Remove-HmsDeliveryFileGuard -Guard $candidateGuard -Label 'Candidate CodeGraph install manifest rollback disposal';$candidateGuard=$null}catch{$rollbackErrors+="Candidate manifest rollback failed: $($_.Exception.Message)"}}
        if($null -ne $ExistingState -and $oldMoved -and $null -ne $ExistingState.Guard){try{if(Test-Path -LiteralPath $CodeGraphManifest){throw "Canonical CodeGraph manifest pathname became occupied during publication rollback: $CodeGraphManifest"};Move-HmsDeliveryFileGuard -Guard $ExistingState.Guard -Destination $CodeGraphManifest -Label 'Previous CodeGraph install manifest restoration';$null=Assert-CodeGraphInstallManifestGuard -Guard $ExistingState.Guard -Manifest $ExistingState.Manifest -Label 'Restored previous CodeGraph install manifest';$oldMoved=$false}catch{$rollbackErrors+="Previous manifest restoration failed: $($_.Exception.Message)"}}
        if($rollbackErrors.Count){throw "CodeGraph manifest publication failed and exact-object rollback was incomplete. Original: $($writeError.Exception.Message). Rollback: $($rollbackErrors -join ' | ')"}
        throw $writeError
    }finally{if($null -ne $candidateGuard -and $null -ne $candidateGuard.Handle){$candidateGuard.Handle.Dispose()}}
}
function Restore-CodeGraphManifestAfterFailure {
    param([Parameter(Mandatory)]$CandidateManifest,[Parameter(Mandatory)]$CandidateGuard,[byte[]]$PreviousBytes,[Parameter(Mandatory)][bool]$HadPrevious)
    if($null -eq $CandidateGuard -or $null -eq $CandidateGuard.Handle -or $CandidateGuard.Handle.IsClosed){throw 'CodeGraph candidate manifest exact guard is unavailable during rollback.'}
    $null=Assert-CodeGraphInstallManifestGuard -Guard $CandidateGuard -Manifest $CandidateManifest -Label 'CodeGraph candidate manifest rollback authority'
    $parent=Split-Path -Parent $CodeGraphManifest
    $discard=Join-Path $parent ('.hms-codegraph-manifest-rollback-discard-'+[guid]::NewGuid().ToString('N')+'.tmp')
    Move-HmsDeliveryFileGuard -Guard $CandidateGuard -Destination $discard -Label 'CodeGraph candidate manifest rollback reservation'
    if($HadPrevious){
        if($null -eq $PreviousBytes){throw 'CodeGraph rollback is missing previous manifest bytes.'}
        $temp=Join-Path $parent ('.hms-codegraph-manifest-restore-'+[guid]::NewGuid().ToString('N')+'.tmp')
        [IO.File]::WriteAllBytes($temp,$PreviousBytes)
        $previousGuard=Open-HmsDeliveryFileGuard -Path $temp -Label 'Previous CodeGraph manifest reconstruction'
        try{
            if(-not(Test-HmsExactBytesEqual -Left (Read-HmsDeliveryFileBytesFromGuard -Guard $previousGuard -Label 'Previous CodeGraph manifest reconstruction') -Right $PreviousBytes)){throw 'Previous CodeGraph manifest reconstruction changed before restoration.'}
            Move-HmsDeliveryFileGuard -Guard $previousGuard -Destination $CodeGraphManifest -Label 'Previous CodeGraph manifest restoration'
            if(-not(Test-HmsExactBytesEqual -Left (Read-HmsDeliveryFileBytesFromGuard -Guard $previousGuard -Label 'Restored previous CodeGraph manifest') -Right $PreviousBytes)){throw 'Restored previous CodeGraph manifest bytes do not match exact pre-transaction bytes.'}
        }finally{if($null -ne $previousGuard -and $null -ne $previousGuard.Handle){$previousGuard.Handle.Dispose()}}
    }
    Remove-HmsDeliveryFileGuard -Guard $CandidateGuard -Label 'Rolled-back candidate CodeGraph manifest disposal'
}

'''
          t=replace_between(t,'function Restore-CodeGraphManifestAfterFailure {','function Repair-CodeGraphRollbackState {',manifest_helpers,'exact manifest publish/rollback helpers')
          t=once(t,'        [Parameter(Mandatory)][bool]$ManifestPublished,\n        $PublishedManifest,','        [Parameter(Mandatory)][bool]$ManifestPublished,\n        $PublishedManifest,\n        $CandidateManifestGuard,','repair manifest guard parameter')
          old_manifest='''    if ($ManifestPublished) {
        try {
            if ($currentBundleState -ceq 'previous') {
                Restore-CodeGraphManifestAfterFailure -CandidateManifest $PublishedManifest -PreviousBytes $PreviousManifestBytes -HadPrevious $true
            }
            elseif ($currentBundleState -ceq 'candidate') {
                # Candidate remains the active verified bundle, so its already-published manifest remains authoritative.
            }
            else {
                Restore-CodeGraphManifestAfterFailure -CandidateManifest $PublishedManifest -PreviousBytes $null -HadPrevious $false
            }
        }
        catch { $rollbackErrors += $_.Exception.Message }
    }
'''
          new_manifest='''    if ($ManifestPublished) {
        try {
            if ($null -eq $CandidateManifestGuard -or $null -eq $CandidateManifestGuard.Handle -or $CandidateManifestGuard.Handle.IsClosed) { throw 'CodeGraph candidate manifest exact guard disappeared before rollback adjudication.' }
            if ($currentBundleState -ceq 'previous') {
                Restore-CodeGraphManifestAfterFailure -CandidateManifest $PublishedManifest -CandidateGuard $CandidateManifestGuard -PreviousBytes $PreviousManifestBytes -HadPrevious $true
            }
            elseif ($currentBundleState -ceq 'candidate') {
                $null=Assert-CodeGraphInstallManifestGuard -Guard $CandidateManifestGuard -Manifest $PublishedManifest -Label 'Preserved candidate CodeGraph install manifest'
                $CandidateManifestGuard.Handle.Dispose();$CandidateManifestGuard.Handle=$null
            }
            else {
                Restore-CodeGraphManifestAfterFailure -CandidateManifest $PublishedManifest -CandidateGuard $CandidateManifestGuard -PreviousBytes $null -HadPrevious $false
            }
        }
        catch { $rollbackErrors += $_.Exception.Message }
    }
'''
          t=once(t,old_manifest,new_manifest,'repair exact manifest adjudication')

          sync_start='''    $existingManifest = Read-ManagedCodeGraphManifest
    $wasInstalled = $null -ne $existingManifest
    $existingManifestBytes = $null
    if ($wasInstalled) {
        $manifestItem = Get-Item -LiteralPath $CodeGraphManifest -Force -ErrorAction Stop
        if ([bool]$manifestItem.PSIsContainer -or [bool]($manifestItem.Attributes -band [IO.FileAttributes]::ReparsePoint)) { throw 'Managed CodeGraph manifest must be a regular file.' }
        $existingManifestBytes = [IO.File]::ReadAllBytes($CodeGraphManifest)
    }
    $arch = Get-CodeGraphArchitecture
'''
          sync_new='''    $existingManifestState = Open-ManagedCodeGraphManifestState
    $existingManifest = if ($null -eq $existingManifestState) { $null } else { $existingManifestState.Manifest }
    $wasInstalled = $null -ne $existingManifest
    $existingManifestBytes = if ($null -eq $existingManifestState) { $null } else { [byte[]]$existingManifestState.Bytes }
    try {
    $arch = Get-CodeGraphArchitecture
'''
          t=once(t,sync_start,sync_new,'sync exact existing manifest state')
          t=once(t,'        $manifestPublished = $false\n        $publishedManifest = $null\n','        $manifestPublished = $false\n        $publishedManifest = $null\n        $manifestPublicationGuard = $null\n','sync manifest publication guard variable')
          publish_old='''                $manifestTemp = Join-Path $CodeGraphRoot ('.hms-codegraph-manifest-' + $transactionId + '.tmp')
                $publishedManifest | ConvertTo-Json | Set-Content -LiteralPath $manifestTemp -Encoding UTF8
                if ($wasInstalled) { [IO.File]::Replace($manifestTemp, $CodeGraphManifest, $null, $true) }
                else { [IO.File]::Move($manifestTemp, $CodeGraphManifest) }
                $manifestPublished = $true
                Assert-CodeGraphTransactionBundle -Path $current -Identity $candidateIdentity
'''
          publish_new='''                $manifestPublicationGuard = Publish-CodeGraphInstallManifest -Manifest $publishedManifest -ExistingState $existingManifestState
                $manifestPublished = $true
                Assert-CodeGraphTransactionBundle -Path $current -Identity $candidateIdentity
'''
          t=once(t,publish_old,publish_new,'exact manifest publication call')
          t=once(t,"                    Remove-CodeGraphTransactionBundle -Path $backup -Identity $backupIdentity\n                }\n","                    Remove-CodeGraphTransactionBundle -Path $backup -Identity $backupIdentity\n                }\n                if ($null -ne $manifestPublicationGuard -and $null -ne $manifestPublicationGuard.Handle) { $manifestPublicationGuard.Handle.Dispose();$manifestPublicationGuard.Handle=$null;$manifestPublicationGuard=$null }\n",'manifest guard commit disposal')
          t=once(t,'-ManifestPublished $manifestPublished -PublishedManifest $publishedManifest -PreviousManifestBytes $existingManifestBytes','-ManifestPublished $manifestPublished -PublishedManifest $publishedManifest -CandidateManifestGuard $manifestPublicationGuard -PreviousManifestBytes $existingManifestBytes','repair call manifest guard')
          t=once(t,'            if (Test-Path -LiteralPath $temp) { Remove-CodeGraphTempRoot -Path $temp -TransactionId $transactionId }\n        }\n    }\n\n    return [pscustomobject]@{ WasInstalled = $wasInstalled; Command = $command }\n}\n','            if ($null -ne $manifestPublicationGuard -and $null -ne $manifestPublicationGuard.Handle) { $manifestPublicationGuard.Handle.Dispose();$manifestPublicationGuard.Handle=$null }\n            if (Test-Path -LiteralPath $temp) { Remove-CodeGraphTempRoot -Path $temp -TransactionId $transactionId }\n        }\n    }\n\n    return [pscustomobject]@{ WasInstalled = $wasInstalled; Command = $command }\n    }\n    finally { if ($null -ne $existingManifestState -and $null -ne $existingManifestState.Guard -and $null -ne $existingManifestState.Guard.Handle) { $existingManifestState.Guard.Handle.Dispose();$existingManifestState.Guard.Handle=$null } }\n}\n','sync manifest-state final disposal')
          save(p,t)

          # Permanent test: lifecycle never executes developer validators.
          p='scripts/Test-HmsTrustRemediations.ps1';t=load(p)
          anchor="    foreach ($literal in @('Assert-NoHiddenIndexState','skip-worktree/assume-unchanged index flags')) {\n        if ($entry.Text -notmatch [regex]::Escape($literal)) { throw \"$($entry.Name) is missing hidden-index trust gate literal: $literal\" }\n    }\n"
          extra=anchor+"    foreach ($forbidden in @('scripts\\\\Test-HmsSkills.ps1','scripts\\\\Test-DeliveryTools.ps1')) {\n        if ($entry.Text -match [regex]::Escape($forbidden)) { throw \"$($entry.Name) production lifecycle still executes developer/CI validator: $forbidden\" }\n    }\n"
          t=once(t,anchor,extra,'lifecycle validator nonexecution regression');save(p,t)

          # Permanent static + dynamic exact-child race regression.
          p='scripts/Test-HmsOwnedTempCleanup.ps1';t=load(p)
          t=t.replace("        '[uint32]0x00010000',","        '[uint32]0x00010001',\n        'GetFileInformationByHandleEx',\n        'EnumerateChildrenByHandle',\n        'Remove-HmsOwnedExactChildren',\n        'child identity changed between enumeration and exact-handle open',")
          append=r'''

# Case C: enumerate an owned child, swap that pathname from another process, and require exact-ID rejection.
$ownedC=New-HmsOwnedTempDirectory -Prefix 'hms-owned-temp-child-swap-' -Label 'child-swap regression'
$victimC=Join-Path $ownedC.Path 'victim.txt'
[IO.File]::WriteAllText($victimC,'owned-original',(New-Object Text.UTF8Encoding($false)))
$probeC=Join-Path $env:TEMP ('hms-owned-child-probe-'+[guid]::NewGuid().ToString('N')+'.txt')
$jobC=$null;$quarantineC=$null
try{
    $env:HMS_TEST_EXACT_CHILD_ENUM_READY=$probeC
    $jobC=Start-Job -ScriptBlock {
        param($Probe)
        $ErrorActionPreference='Stop'
        $limit=(Get-Date).AddSeconds(20)
        while(-not(Test-Path -LiteralPath $Probe)){if((Get-Date)-gt$limit){throw 'Timed out waiting for exact-child enumeration probe.'};Start-Sleep -Milliseconds 40}
        $path=[IO.File]::ReadAllText($Probe)
        $moved=Join-Path (Split-Path -Parent $path) 'owned-original-moved.txt'
        Rename-Item -LiteralPath $path -NewName (Split-Path -Leaf $moved) -ErrorAction Stop
        [IO.File]::WriteAllText($path,'FOREIGN-REPLACEMENT-MUST-SURVIVE',(New-Object Text.UTF8Encoding($false)))
    } -ArgumentList $probeC
    $rejected=$false
    try{Remove-HmsOwnedTempDirectory -Owned $ownedC -Label 'child-swap regression'}catch{if($_.Exception.Message -match 'child identity changed between enumeration and exact-handle open'){$rejected=$true}else{throw}}
    $null=Wait-Job -Job $jobC -Timeout 20
    if($jobC.State -ne 'Completed'){throw "Child-swap attacker job did not complete. State=$($jobC.State)"}
    Receive-Job -Job $jobC -ErrorAction Stop | Out-Null
    if(-not$rejected){throw 'Exact-child cleanup did not reject the swapped child pathname.'}
    $probed=[IO.File]::ReadAllText($probeC);$quarantineC=Split-Path -Parent $probed
    if([IO.File]::ReadAllText((Join-Path $quarantineC 'victim.txt')) -cne 'FOREIGN-REPLACEMENT-MUST-SURVIVE'){throw 'Exact-child cleanup modified/deleted the foreign replacement.'}
    if([IO.File]::ReadAllText((Join-Path $quarantineC 'owned-original-moved.txt')) -cne 'owned-original'){throw 'Exact-child cleanup lost the enumerated original after hostile swap.'}
}
finally{
    $env:HMS_TEST_EXACT_CHILD_ENUM_READY=$null
    if($null -ne $jobC){Remove-Job -Job $jobC -Force -ErrorAction SilentlyContinue}
    if($null -ne $ownedC.Guard){$ownedC.Guard.Dispose();$ownedC.Guard=$null}
    foreach($x in @($probeC,$ownedC.Path,$quarantineC)){if(-not[string]::IsNullOrWhiteSpace([string]$x)-and(Test-Path -LiteralPath $x)){Remove-Item -LiteralPath $x -Recurse -Force -ErrorAction SilentlyContinue}}
}
Write-Host 'PASS: exact child file-ID binding rejects hostile pathname substitution without deleting the replacement.'
'''
          t=t.rstrip()+append+'\n';save(p,t)

          # Late-trust permanent static contracts plus marker/manifest hostile occupancy regressions.
          p='scripts/Test-HmsLateTrustBoundaries.ps1';t=load(p)
          marker="foreach($literal in @('HmsDeliveryExactFsNative','Invoke-HmsDeliveryExactDirectoryRemoval','reservedBackupGuard','Move-HmsDeliveryDirectoryGuard -Guard $reservedBackupGuard -Destination $CurrentPath')){if($deliveryText-notmatch[regex]::Escape($literal)){throw \"Delivery tools are missing exact-object contract: $literal\"}}\n"
          replacement="foreach($literal in @('HmsDeliveryExactFsNative','Invoke-HmsDeliveryExactDirectoryRemoval','reservedBackupGuard','Move-HmsDeliveryDirectoryGuard -Guard $reservedBackupGuard -Destination $CurrentPath','GetFileInformationByHandleEx','EnumerateChildrenByHandle','Remove-HmsDeliveryExactChildren','Open-HmsDeliveryFileGuard','Move-HmsDeliveryFileGuard','Publish-CodeGraphInstallManifest','ExpectedPreviousIdentity')){if($deliveryText-notmatch[regex]::Escape($literal)){throw \"Delivery tools are missing exact-object contract: $literal\"}}\n"
          t=once(t,marker,replacement,'late delivery exact file/child contracts')
          unmarker="foreach($literal in @('HmsUninstallExactFsNative','Invoke-HmsUninstallExactDirectoryRemoval','DeleteByHandle')){if($uninstallText-notmatch[regex]::Escape($literal)){throw \"Uninstall is missing exact-object cleanup contract: $literal\"}}\n"
          unrepl="foreach($literal in @('HmsUninstallExactFsNative','Invoke-HmsUninstallExactDirectoryRemoval','DeleteByHandle','GetFileInformationByHandleEx','EnumerateChildrenByHandle','Remove-HmsUninstallExactChildren')){if($uninstallText-notmatch[regex]::Escape($literal)){throw \"Uninstall is missing exact-object cleanup contract: $literal\"}}\n"
          t=once(t,unmarker,unrepl,'late uninstall child contract')
          badline="foreach($bad in @('Remove-Item -LiteralPath $quarantine -Recurse -Force','Remove-Item -LiteralPath $q -Recurse -Force')){if($deliveryText-match[regex]::Escape($bad)){throw \"Delivery exact cleanup reverted to root-path recursive deletion: $bad\"}}\n"
          t=once(t,badline,badline+"foreach($entry in @([pscustomobject]@{Name='delivery';Text=$deliveryText},[pscustomobject]@{Name='uninstall';Text=$uninstallText},[pscustomobject]@{Name='builder';Text=$builderText})){if($entry.Text-match[regex]::Escape('Remove-Item -LiteralPath $child.FullName -Recurse')){throw \"$($entry.Name) exact cleanup still recursively deletes mutable child pathnames.\"}}\n",'late no child pathname recursion')
          race=r'''

# Fresh-review regression: hostile occupancy during marker/manifest canonical gaps must be preserved, never overwritten.
. $deliveryPath -SkipCodeGraph -SkipThreeLevelDelivery
$raceRoot=Join-Path $env:TEMP ('hms-codegraph-exact-file-race-'+[guid]::NewGuid().ToString('N'))
$markerBundle=Join-Path $raceRoot 'bundle';$markerProbe=Join-Path $raceRoot 'marker-probe.txt';$manifestProbe=Join-Path $raceRoot 'manifest-probe.txt'
$markerJob=$null;$manifestJob=$null;$manifestState=$null
try{
    New-Item -ItemType Directory -Force -Path $markerBundle | Out-Null
    [IO.File]::WriteAllText((Join-Path $markerBundle 'payload.txt'),'payload',(New-Object Text.UTF8Encoding($false)))
    $tree=Get-CodeGraphBundleTreeSha256 -Path $markerBundle
    $old=New-CodeGraphBundleIdentity -TransactionId ('1'*32) -Role 'candidate' -Version 'old' -Tag 'v-old' -Commit 'old' -Asset 'old.zip' -Sha256 ('a'*64) -BundleTreeSha256 $tree
    $new=New-CodeGraphBundleIdentity -TransactionId ('2'*32) -Role 'backup' -Version 'old' -Tag 'v-old' -Commit 'old' -Asset 'old.zip' -Sha256 ('a'*64) -BundleTreeSha256 $tree
    Write-CodeGraphBundleMarker -Path $markerBundle -Identity $old
    $env:HMS_TEST_CODEGRAPH_MARKER_RESERVED_READY=$markerProbe
    $markerJob=Start-Job -ScriptBlock {param($Probe)while(-not(Test-Path -LiteralPath $Probe)){Start-Sleep -Milliseconds 40};$p=[IO.File]::ReadAllText($Probe);[IO.File]::WriteAllText($p,'FOREIGN-MARKER-MUST-SURVIVE',(New-Object Text.UTF8Encoding($false)))} -ArgumentList $markerProbe
    $markerRejected=$false
    try{Write-CodeGraphBundleMarker -Path $markerBundle -Identity $new -ExpectedPreviousIdentity $old}catch{if($_.Exception.Message -match 'destination is occupied|rollback was incomplete'){$markerRejected=$true}else{throw}}
    $null=Wait-Job -Job $markerJob -Timeout 20;Receive-Job -Job $markerJob -ErrorAction Stop|Out-Null
    if(-not$markerRejected){throw 'Marker publication did not fail closed when canonical pathname was occupied during exact reservation.'}
    if([IO.File]::ReadAllText((Join-Path $markerBundle $CodeGraphBundleMarkerName)) -cne 'FOREIGN-MARKER-MUST-SURVIVE'){throw 'Marker publication overwrote/deleted hostile canonical replacement.'}

    $CodeGraphRoot=Join-Path $raceRoot 'codegraph';New-Item -ItemType Directory -Force -Path $CodeGraphRoot|Out-Null;$CodeGraphManifest=Join-Path $CodeGraphRoot 'hms-codegraph-install.json'
    $previous=[ordered]@{managed_by=$ManagedBy;version='old';tag='v-old';commit='old';asset='old.zip';sha256=('b'*64);bundle_transaction_id=('3'*32);bundle_tree_sha256=('c'*64)}
    [IO.File]::WriteAllText($CodeGraphManifest,(($previous|ConvertTo-Json)+"`n"),(New-Object Text.UTF8Encoding($false)))
    $manifestState=Open-ManagedCodeGraphManifestState
    $candidate=[ordered]@{managed_by=$ManagedBy;version='new';tag='v-new';commit='new';asset='new.zip';sha256=('d'*64);bundle_transaction_id=('4'*32);bundle_tree_sha256=('e'*64)}
    $env:HMS_TEST_CODEGRAPH_MANIFEST_RESERVED_READY=$manifestProbe
    $manifestJob=Start-Job -ScriptBlock {param($Probe)while(-not(Test-Path -LiteralPath $Probe)){Start-Sleep -Milliseconds 40};$p=[IO.File]::ReadAllText($Probe);[IO.File]::WriteAllText($p,'FOREIGN-MANIFEST-MUST-SURVIVE',(New-Object Text.UTF8Encoding($false)))} -ArgumentList $manifestProbe
    $manifestRejected=$false
    try{$g=Publish-CodeGraphInstallManifest -Manifest $candidate -ExistingState $manifestState;if($null-ne$g){$g.Handle.Dispose()}}catch{if($_.Exception.Message -match 'destination is occupied|rollback was incomplete'){$manifestRejected=$true}else{throw}}
    $null=Wait-Job -Job $manifestJob -Timeout 20;Receive-Job -Job $manifestJob -ErrorAction Stop|Out-Null
    if(-not$manifestRejected){throw 'Manifest publication did not fail closed when canonical pathname was occupied during exact reservation.'}
    if([IO.File]::ReadAllText($CodeGraphManifest) -cne 'FOREIGN-MANIFEST-MUST-SURVIVE'){throw 'Manifest publication overwrote/deleted hostile canonical replacement.'}
}
finally{
    foreach($n in @('HMS_TEST_CODEGRAPH_MARKER_RESERVED_READY','HMS_TEST_CODEGRAPH_MANIFEST_RESERVED_READY')){Remove-Item -LiteralPath "Env:\$n" -ErrorAction SilentlyContinue}
    foreach($j in @($markerJob,$manifestJob)){if($null-ne$j){Remove-Job -Job $j -Force -ErrorAction SilentlyContinue}}
    if($null-ne$manifestState -and $null-ne$manifestState.Guard -and $null-ne$manifestState.Guard.Handle){$manifestState.Guard.Handle.Dispose()}
    if(Test-Path -LiteralPath $raceRoot){Remove-Item -LiteralPath $raceRoot -Recurse -Force -ErrorAction SilentlyContinue}
}
Write-Host 'PASS: exact marker/manifest publication preserves hostile canonical replacements and fails closed.'
'''
          t=t.rstrip()+race+'\n';save(p,t)

      - name: Create exact local V11 candidate and run full permanent structural suite
        shell: pwsh
        run: |
          $ErrorActionPreference='Stop'
          $targets=@(
            'install.ps1','update.ps1','scripts/Sync-DeliveryTools.ps1','scripts/Build-HmsCompositeSkill.ps1','scripts/Copy-HmsCommittedGitPath.ps1','uninstall.ps1',
            'scripts/Test-HmsTrustRemediations.ps1','scripts/Test-HmsOwnedTempCleanup.ps1','scripts/Test-HmsLateTrustBoundaries.ps1'
          )
          Remove-Item -LiteralPath $env:SELF_PATH -Force
          $allowed=@($targets+$env:SELF_PATH)
          $changed=@(& git status --porcelain=v1 --untracked-files=all | ForEach-Object {$_.Substring(3).Replace('\','/')} | Sort-Object -Unique)
          $unexpected=@($changed|Where-Object{$_ -notin $allowed})
          if($unexpected.Count){throw "Unexpected V11 author paths: $($unexpected -join ', ')"}
          foreach($path in $allowed){if($path -notin $changed){throw "Expected V11 path not changed: $path"}}
          & git diff --check
          if($LASTEXITCODE -ne 0){throw 'git diff --check rejected V11.'}
          foreach($path in @('install.ps1','update.ps1','scripts/Sync-DeliveryTools.ps1','scripts/Build-HmsCompositeSkill.ps1','scripts/Copy-HmsCommittedGitPath.ps1','uninstall.ps1','scripts/Test-HmsTrustRemediations.ps1','scripts/Test-HmsOwnedTempCleanup.ps1','scripts/Test-HmsLateTrustBoundaries.ps1')){
            $tokens=$null;$errs=$null;[Management.Automation.Language.Parser]::ParseFile((Join-Path $env:GITHUB_WORKSPACE $path),[ref]$tokens,[ref]$errs)|Out-Null
            if(@($errs).Count){throw "Parser rejected $path : $((@($errs)|ForEach-Object Message)-join ' | ')"}
          }
          & git config user.name 'github-actions[bot]'
          & git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          & git add -- $allowed
          & git commit -m 'Close fresh-review lifecycle and exact-file cleanup P1s'
          if($LASTEXITCODE -ne 0){throw 'Local V11 candidate commit failed.'}
          $net=@(& git diff --name-only $env:CLEAN_BASELINE HEAD)
          if($net.Count -ne $targets.Count -or @($net|Where-Object{$_ -notin $targets}).Count){throw "V11 net scope mismatch: $($net -join ', ')"}
          Write-Host "LOCAL_CANDIDATE=$(((& git rev-parse HEAD)-join '').Trim().ToLowerInvariant())"
          Write-Host "LOCAL_TREE=$(((& git rev-parse 'HEAD^{tree}')-join '').Trim().ToLowerInvariant())"
          & (Join-Path $env:GITHUB_WORKSPACE 'scripts\Test-HmsSkills.ps1') -RepoRoot $env:GITHUB_WORKSPACE
          if($LASTEXITCODE -ne 0){throw "Test-HmsSkills failed with exit code $LASTEXITCODE"}

      - name: Prove lifecycle no longer executes developer validators
        shell: pwsh
        run: |
          $ErrorActionPreference='Stop'
          foreach($path in @('install.ps1','update.ps1')){
            $text=[IO.File]::ReadAllText((Join-Path $env:GITHUB_WORKSPACE $path))
            foreach($bad in @('scripts\Test-HmsSkills.ps1','scripts\Test-DeliveryTools.ps1')){if($text-match[regex]::Escape($bad)){throw "$path still executes developer validator: $bad"}}
          }
          Write-Host 'PASS: production install/update have no developer-validator execution boundary.'

      - name: Prepare nine verified GitHub blobs
        shell: pwsh
        run: |
          $ErrorActionPreference='Stop'
          $targets=@(
            'install.ps1','update.ps1','scripts/Sync-DeliveryTools.ps1','scripts/Build-HmsCompositeSkill.ps1','scripts/Copy-HmsCommittedGitPath.ps1','uninstall.ps1',
            'scripts/Test-HmsTrustRemediations.ps1','scripts/Test-HmsOwnedTempCleanup.ps1','scripts/Test-HmsLateTrustBoundaries.ps1'
          )
          $headers=@{Authorization="Bearer $env:GH_TOKEN";Accept='application/vnd.github+json';'X-GitHub-Api-Version'='2022-11-28'}
          $repo='hoangminhsang989/HMS-Skills-Codex'
          foreach($path in $targets){
            $bytes=[IO.File]::ReadAllBytes((Join-Path $env:GITHUB_WORKSPACE $path))
            $payload=@{content=[Convert]::ToBase64String($bytes);encoding='base64'}|ConvertTo-Json -Compress
            $result=Invoke-RestMethod -Method Post -Uri "https://api.github.com/repos/$repo/git/blobs" -Headers $headers -Body $payload -ContentType 'application/json'
            $expected=((& git hash-object --no-filters -- $path)-join '').Trim().ToLowerInvariant()
            if([string]$result.sha -cne $expected){throw "GitHub blob mismatch for $path. Expected $expected, got $($result.sha)"}
            Write-Host "PREPARED_BLOB $path $expected"
          }
          Write-Host "QUALIFIED_LOCAL_HEAD=$(((& git rev-parse HEAD)-join '').Trim().ToLowerInvariant())"
          Write-Host "QUALIFIED_LOCAL_TREE=$(((& git rev-parse 'HEAD^{tree}')-join '').Trim().ToLowerInvariant())"
          Write-Host 'DETACHED_BLOBS_ONLY=YES'
